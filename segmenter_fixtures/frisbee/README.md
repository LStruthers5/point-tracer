# Frisbee Segmenter Fixtures

Each `Manual_Disc_N` folder contains one manually labeled activity:

- `source.gpx`: original GPX activity file
- `corrected_boundaries.json`: corrected segment boundaries exported from the frontend editor

The JSON `activity_name` and `source_file` fields are normalized to the matching `Manual_Disc_N` activity name.
